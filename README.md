# COM-304-RADARS-BRANCH

## Info
This branch uses the same logistic regresiion model but with all of kaspers doppler filtering

## Usage
data_exploration/model/jerry_detector_doppler.joblib has the correct model version

realtime_streaming_task_3 has all the classifier implem code, mostly the 2 under the sections with
            # ----------------------------------------------------
            # NOTE: Jan - ...
            # -------------------------------------------------------
import joblib is required to read the model files